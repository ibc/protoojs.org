source: .

exclude: [NO_GIT, Gemfile, Gemfile.lock]

markdown: kramdown

kramdown:
  input: GFM

gems:
  - jekyll-compass
  - singularitygs
  - breakpoint

defaults:
  -
    scope:
      path: ''
    values:
      layout: layout

